# FAQ

## `Unexpected 'ExpressionStatement' in code: only import/exports are supported`

MDX 要求 Import 语句与 Jsx 调用不能在相邻的两行

```jsx error
import { Button } from "./components/Button";

<Button />;
```

```jsx success
import { Button } from "./components/Button";
<Button />;
```
