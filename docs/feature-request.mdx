# Feature Request

- [ ] code block 支持 line number, highlight specfic line, success and error code blocks
- [x] Support custom sidebar configs
- [x] sandbox for each js live block
- [x] 404 and / path handling
- [ ] jsx live but with error code or error rendering
- [ ] highlightjs removed from client, use dynamic code block render from mdx like code sandbox
- [ ] highlightjs safe mode
- [ ] Core.tmp logic fine tune
- [ ] i18n support
- [ ] support for mobile component development, iframe props table, and need a new syntax
- [ ] review all documents

## code live Sandbox

- 把 js live 的代码块抽成一个虚拟模块，以 mdx 结尾
- 抽成 string，塞入 conext
- 用 viteplugin 读取 context, 将 import path 与 模块内容对应
- 由于 path 以 mdx 结尾，期望 mdx loader 可以处理
- 若不处理，手动 Parse 成 mdx react component
- 把 js live 的代码节点直接删除，用`<SandBoxHashCode/>` 替换
- 在 ast 头部，加入 n 个 import SandBoxHashCode 的 import 节点
